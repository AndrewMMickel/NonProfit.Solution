@{
    Layout = "_Layout";
}

@using NonProfitTracker.Models;
@model NonProfitTracker.Models.BoardMember

<h2>Edit</h2>

<h4>Edit this task: @Html.DisplayFor(model => model.Name)</h4>

@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.BoardMemberId)

    @Html.LabelFor(model => model.Name)
    @Html.EditorFor(model => model.Name)

    @Html.LabelFor(model => model.Age)
    @Html.EditorFor(model => model.Age)<br>

    @Html.LabelFor(model => model.Description)
    @Html.EditorFor(model => model.Description)<br>

    <input type="submit" value="Save" />
}

<p>@Html.ActionLink("Back to list", "Index")</p>